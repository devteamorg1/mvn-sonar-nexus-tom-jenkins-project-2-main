<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>0.0.1-Snapshot</version> 
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.example</groupId>
	<artifactId>JavaWebApp</artifactId>
	<packging>war</packging>
	<version>0.0.1</version>
	<name>JavaWebApp</name>
	<description>JavaWebApp project for Spring Boot</description>

	<properties>
		<java.version>1.8</java.version>
	    <sonar.host.url>http://3.86.104.6:9000/</sonar.host.url>
        <sonar.login>squ_9ed1e6f81bf9b05c2e64206e6b69d27f19ac7e73</sonar.login> 
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
			<exclusions>
				<exclusion>
					<groupId>org.junit.vintage</groupId>
					<artifactId>junit-vintage-engine</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!--plugin>
   				<groupId>org.apache.maven.plugins</groupId>
   				<artifactId>maven-deploy-plugin</artifactId>
   				<version>${maven-deploy-plugin.version}</version>
   			</plugin-->
		</plugins>
	</build>

	<distributionManagement>
    <snapshotRepository>
      <id>nexusdeploymentrepo</id>
	  <name>hdfcbank-Snapshot Nexus Repository</name>
      <url>http://3.80.53.84:8081/repository/hdfcbank-Snapshot/</url> <!-- Update the IP/Address to Your Nexus VM IP -->
    </snapshotRepository>											  <!-- Mmake sure to create the "maven-snapshots" repo in Nexus -->
	<repository>
	  <id>nexusdeploymentrepo</id>
	  <name>hdfcbank-Release Nexus Repository</name>
      <url>http://3.80.53.84:8081/repository/hdfcbank-Release/</url> <!-- Update the IP/Address to Your Nexus VM IP -->
    </repository>													 <!-- Also make sure you have the above two or one repositories created -->
  
  </distributionManagement>

</project>
